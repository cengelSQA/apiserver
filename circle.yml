machine:
  ruby:
    version: 2.3.0
checkout:
  post:
    - git submodule sync
    - git submodule update --init

test:
  pre:
    - bundle install --gemfile ./apitest/Gemfile
  override:
    - ruby ./apitest/test_api.rb
